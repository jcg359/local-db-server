{
  "name": "local-db-server",
  "version": "1.0.0",
  "description": "Local server that executes named queries against configured database connections",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "bash test.sh"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "mssql": "^10.0.4",
    "mysql2": "^3.6.5"
  }
}
